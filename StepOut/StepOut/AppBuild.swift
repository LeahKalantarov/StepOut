import Foundation

/// Bumped when tutor UI changes so you can confirm the iPad is running the latest build.
enum AppBuild {
    static let marker = "NEW BUILD · 0a6323e"
    static let isNewTutorUI = true
}
